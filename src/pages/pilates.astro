---
import { getEntry, render } from 'astro:content';
import PageLayout from '../layouts/PageLayout.astro';

const page = await getEntry('pages', 'pilates');
const { Content } = await render(page);
---

<PageLayout page={page}>
  <Content />
</PageLayout>
