---
import { getEntry, render } from 'astro:content';
import PageLayout from '../../layouts/PageLayout.astro';

const page = await getEntry('pages', 'palestra');
const { Content } = await render(page);
---

<PageLayout page={page}>
  <Content />
</PageLayout>
