---
import { getEntry, render } from 'astro:content';
import PageLayout from '../../layouts/PageLayout.astro';

// URL reale gerarchico (post_parent = palestra): /palestra/sala-pesi/, vedi slug-map.js
const page = await getEntry('pages', 'palestra/sala-pesi');
const { Content } = await render(page);
---

<PageLayout page={page}>
  <Content />
</PageLayout>
