---
import { getEntry, render } from 'astro:content';
import PageLayout from '../layouts/PageLayout.astro';

const page = await getEntry('pages', 'personal-training');
const { Content } = await render(page);
---

<PageLayout page={page}>
  <Content />
</PageLayout>
